document.addEventListener('DOMContentLoaded', function () {    if (typeof wc_checkout_params !== 'undefined') {        let clienteNome = localStorage.getItem('btr_cliente_nome');        let clienteEmail = localStorage.getItem('btr_cliente_email');        if (clienteNome) {            let nomeInput = document.querySelector('#billing_first_name');            if (nomeInput) nomeInput.value = clienteNome;        }        if (clienteEmail) {            let emailInput = document.querySelector('#billing_email');            if (emailInput) emailInput.value = clienteEmail;        }        // Rimuovi i dati dal LocalStorage dopo il caricamento        localStorage.removeItem('btr_cliente_nome');        localStorage.removeItem('btr_cliente_email');    }});