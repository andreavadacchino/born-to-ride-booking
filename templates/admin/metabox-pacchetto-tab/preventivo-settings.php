<div class="btr-section collapsible active">    <h2>Impostazioni Preventivo</h2>    <div class="section-content">        <div class="btr-help-box" style="background:#fefefe; border-left:4px solid #0073aa; padding:15px 20px; margin-bottom:20px; border-radius:6px;">            <h3 style="margin-top:0;">ℹ️ Come funzionano queste impostazioni</h3>            <p>In questa sezione puoi configurare due parametri fondamentali per la gestione dei preventivi:</p>            <ul style="padding-left: 20px; list-style-type: disc;">                <li><strong>Giorni prima chiusura prenotazioni:</strong> definisce quanti giorni prima della data di partenza le prenotazioni verranno automaticamente chiuse.</li>                <li><strong>Durata validità preventivo:</strong> indica per quanti giorni un preventivo generato rimane valido per l’utente prima che scada.</li>            </ul>            <p>Queste impostazioni aiutano a evitare prenotazioni tardive o preventivi non più aggiornati.</p>        </div>        <?php        // Valori di default        $close_days     = get_post_meta($post->ID, 'btr_booking_close_days', true);        $close_days     = is_numeric($close_days) ? $close_days : 3;        $validity_days  = get_post_meta($post->ID, 'btr_quote_validity_days', true);        $validity_days  = is_numeric($validity_days) ? $validity_days : 7;        ?>        <div class="btr-field-group btr-room-row row-children">            <div class="btr-field-group" style="flex-direction: row; align-items: center;">                <label for="btr_booking_close_days"><strong><?php _e('Giorni prima chiusura prenotazioni', 'born-to-ride-booking'); ?></strong></label>                <input type="number" id="btr_booking_close_days" name="btr_booking_close_days" min="0" value="<?php echo esc_attr($close_days); ?>" />                <small>Quanti giorni prima della data di partenza si chiuderanno automaticamente le prenotazioni.</small>            </div>        </div>        <div class="btr-field-group btr-room-row row-children">            <div class="btr-field-group" style="flex-direction: row; align-items: center;">                <label for="btr_quote_validity_days"><strong><?php _e('Durata validità preventivo (giorni)', 'born-to-ride-booking'); ?></strong></label>                <input type="number" id="btr_quote_validity_days" name="btr_quote_validity_days" min="1" value="<?php echo esc_attr($validity_days); ?>" />                <small>Per quanti giorni un preventivo rimane valido prima di scadere.</small>            </div>        </div>    </div></div>